app:
  security:
    jwt:
      secret: ${JWT_SECRET:?JWT_SECRET obrigatório}
  sync:
    scheduler:
      enabled: ${APP_SYNC_SCHEDULER_ENABLED:false}
  cors:
    allowed-origins: "${CORS_ALLOWED_ORIGINS:}"
